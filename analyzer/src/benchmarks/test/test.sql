CREATE TABLE Accounts (
    id          INT ,
    balance     INT ,
    name		VARCHAR(10),
    PRIMARY KEY (id)
);



CREATE TABLE anotherTable (
    id          INT ,
    balance     INT ,
    name		VARCHAR(10),
    PRIMARY KEY (id)
);


